// Positional scales for binning continuous data (x & y)
H.gg.prototype.scale_x_binned = function (x) {
    this.x = x;
    return this;
};

// Positional scales for binning continuous data (x & y)
H.gg.prototype.scale_y_binned = function (x) {
    this.x = x;
    return this;
};